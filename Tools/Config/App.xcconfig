MARKETING_VERSION = 0.9.1
CURRENT_PROJECT_VERSION = 1

// Release builds must omit get-task-allow for notarization.
CODE_SIGN_INJECT_BASE_ENTITLEMENTS[config=Release] = NO

NIFRO_BUNDLE_ID = com.pathgao.nifro
NIFRO_URL_SCHEME = nifro
