{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/PathGao/Nifro/blob/main/sites/schema.json",
  "title": "Nifro site entry",
  "description": "One curated website that works well as a desktop wallpaper. One YAML file per site in sites/.",
  "type": "object",
  "required": [
    "name",
    "url",
    "description",
    "tags",
    "backend",
    "source"
  ],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 60,
      "description": "Display name shown in the in-app gallery."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://",
      "description": "The URL Nifro loads. Include any query parameters that are part of the recommended setup (kiosk mode, transparent background, etc.)."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "One sentence. What the user sees on their desktop."
    },
    "tags": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "3d",
          "ambient",
          "art",
          "calendar",
          "clock",
          "dashboard",
          "data",
          "gaming",
          "live",
          "map",
          "music",
          "nature",
          "news",
          "personal",
          "photo",
          "screensaver",
          "space",
          "weather"
        ]
      }
    },
    "backend": {
      "type": "string",
      "enum": [
        "snapshot",
        "live"
      ],
      "description": "Nothing reads this field today. The two rendering backends it selected between were built, measured and taken back out; see the ‘Lower idle-wallpaper cost’ experiment in docs/ROADMAP.md. It is recorded so the answer does not have to be worked out again for every site if that experiment returns. snapshot: the page settles and then holds still, so it would not need to keep rendering. live: the page animates, or being able to click it is the point. Default to snapshot; only say live when animation or interaction is what the page is for."
    },
    "backendNote": {
      "type": "string",
      "maxLength": 300,
      "description": "Why this backend was chosen, when it is not obvious. Required in spirit for every `live` entry."
    },
    "reloadInterval": {
      "type": "integer",
      "minimum": 1,
      "description": "Seconds between reloads of the live page. Omit unless a reload is the only way the page shows anything new — a clock keeps its own time, and a drawing has nothing to show."
    },
    "css": {
      "type": "string",
      "description": "Custom CSS injected into the page. Typically hides nav bars and makes the background transparent."
    },
    "js": {
      "type": "string",
      "description": "Custom JavaScript injected into the page after load."
    },
    "requiresLogin": {
      "type": "boolean",
      "default": false,
      "description": "true if the user must be signed in to the site for it to show anything useful."
    },
    "screenshot": {
      "type": "string",
      "description": "Path or URL of a preview image. Leave unset until an image exists."
    },
    "source": {
      "type": "string",
      "minLength": 1,
      "description": "Where this entry came from, and anything a reader needs in order to adapt it — which part of the URL to swap, why a flag is there, what goes stale."
    },
    "audio": {
      "type": "string",
      "enum": [
        "muted",
        "unmuted"
      ],
      "description": "The sound setting a site starts with when it is added. Only a starting value: from then on it belongs to the user's copy of the website and is changed from the Sound item in the menu, which this never overrides. Omit for muted, which is what a wallpaper wants unless it is a stream."
    },
    "featured": {
      "type": "integer",
      "minimum": 1,
      "description": "Where this entry sits in the list the app ships with, counting from 1. Omit it and the entry does not ship. A number rather than true, because the order is the decision: the entry ranked 1 is the wallpaper somebody sees before they have chosen anything, and on a second display they see the entry ranked 2. Ranks must be unique across sites/ — Tools/validate-sites.py rejects a duplicate, because two entries claiming one position leave the order to whatever the sort happens to do. Keep this list very short: every entry is one somebody has to delete if they do not want it."
    },
    "zoom": {
      "type": "object",
      "description": "Fill the wallpaper with one part of the page instead of the whole thing. Given as a place and a magnification rather than a rectangle, so the same entry works on any screen shape: each display works out its own rectangle around that place.",
      "required": [
        "centerX",
        "centerY",
        "scale"
      ],
      "additionalProperties": false,
      "properties": {
        "centerX": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Middle of the region across the page, 0 at the left edge and 1 at the right."
        },
        "centerY": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Middle of the region down the page, 0 at the top edge and 1 at the bottom."
        },
        "scale": {
          "type": "number",
          "minimum": 1,
          "maximum": 20,
          "description": "How many times that part is enlarged to fill the screen."
        }
      }
    }
  }
}
