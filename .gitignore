# macOS
.DS_Store

# Xcode
xcuserdata/
.xcode-build/

# SwiftPM, used by the test target
.build/

# Release staging
.release/

# Local virtualenv for the site registry scripts
.venv/

# Signing material. The release certificate is a private key; it belongs in a backup and a
# repo secret, never in the tree.
*.p12
*.keychain-db
